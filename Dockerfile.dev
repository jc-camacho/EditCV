FROM node:20-alpine

# Alpine only ships busybox sh; Claude Code needs bash to run commands
RUN apk add --no-cache bash git

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev"]
